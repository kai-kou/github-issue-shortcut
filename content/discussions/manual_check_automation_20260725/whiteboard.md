<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: 実機手動確認チェックリスト（起票フロー）の自動化スコープ確定（Issue #148）

- 議題ID: `manual_check_automation_20260725`
- 論点: 実機確認待ち Issue（#145/#16/#15）の Done Criteria を消化するために人手で実施した『オンライン系チェックリスト』を、どこまで自動化すべきかを敵対的にレビューして確定する。チェックリスト実体: (1) リポジトリ検索欄に `バグ修正 #repo名` と入力 → `#` トークンがハイライトされ一覧が絞り込まれる、(2) リポジトリをタップ → 残りの自由文『バグ修正』がタイトル初期値へ引き継がれる、(3) タイトル欄に `テスト @b` → 入力欄直下にラベル候補チップ（bug 等）が出る、(4) 候補タップ → `テスト @bug ` に確定 + ラベルがチェック済み + キーボードが閉じない、(5) `@bug` 確定後は候補が消える、(6) ラベルチップのタップ解除でテキストとラベル選択が連動して消える、(7) コールドスタート（WebAPK standalone）からタイトルのみ起票し送信完了まで 5 秒以内をストップウォッチ実測。決めるべきは、(a) 既存 E2E（e2e/smart-input.spec.ts・kpi.spec.ts・shortcuts.spec.ts 他 18 spec）が既にカバーしている範囲と本当に未カバーの範囲の切り分け（重複テストを増やさない）、(b) エミュレーション（Playwright Pixel 7・モック GitHub）で十分な項目と実機でしか担保できない項目（実指タップ精度・実機フォント視認性・ソフトキーボード挙動・WebAPK standalone コールドスタート・Service Worker Background Sync）の線引き、(c) 5 秒 KPI の自動化形態（e2e/kpi.spec.ts は既に外形計測 PoC を持ち『実機体感の代替ではない』と明記している。回帰ガードとして閾値 assert を強めるのか、実測を KPI 正本として残すのか）、(d) 残る手動確認の『チェックリスト生成・結果の Issue コメント記録』まで自動化するか（新規ツール追加の是非）、(e) 実行経路（ci.yml の E2E に載せるか、smoke.yml で本番に対して回すか）。制約: 追加ライブラリを増やさない（docs/design/design-guidelines.md D-6）・YAGNI・タスク外リファクタ禁止。
- 参加者: `e2e-coverage-auditor`, `real-device-realist`, `automation-maximalist`, `yagni-guard`
- 投稿数: 5
- 更新: 2026-07-25T13:45:28+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `lead` — メモ
<sub>2026-07-25T13:40:07+09:00</sub>

## 事前調査（lead・事実共有）

サブエージェントによる既存資産の実測調査結果。**各自でファイルを読んで検証すること**（本メモの誤りを見つけたら指摘してよい）。

### 手動チェックリスト項目 × 既存 E2E カバレッジ

| 手動項目 | 既存 E2E | 状態 |
|---------|---------|------|
| `#repo` ハイライト・一覧絞り込み | `e2e/smart-input.spec.ts:211` | ✅ 機能面カバー（色ハイライトの視覚検証はなし） |
| リポジトリ選択時のタイトル引き継ぎ | `e2e/smart-input.spec.ts:211,244` | ✅ カバー |
| `@label` ハイライト・チップ・タップ解除 | `e2e/smart-input.spec.ts:13,104,171` | ✅ 機能面カバー |
| `@b` 候補表示・候補タップ確定（#145/#146） | `e2e/smart-input.spec.ts:49` | ✅ カバー |
| **候補タップでフォーカス（キーボード）が維持される** | `e2e/smart-input.spec.ts:86` 付近にフォーカス assert なし | ❌ **未カバー** |
| タイトルのみ起票 5 秒以内 | `e2e/kpi.spec.ts:128,187,297` | ⚠️ 外形計測はあるが **assert 閾値は 10 秒**（NFR-2 相当）。5 秒は未 assert |
| オフラインキュー再送で **重複しない** こと | `e2e/offline-queue.spec.ts:11` | ⚠️ 再送自体はカバー。**重複なしの検証は未実装**（mock-github に作成回数カウンタなし） |

### 実行基盤の事実

- `playwright.config.ts:22-31`: baseURL は `http://localhost:8789` 固定・Pixel 7 エミュレーション。**projects 分岐・本番 baseURL 切替は未実装**
- `playwright.config.ts:11-20`: `fullyParallel: false` / `workers: 1` / CI のみ `retries: 2`（テスト追加は実行時間に直結）
- `webServer`: `e2e/mock-github.mjs`（ローカル専用モック GitHub）+ `wrangler dev`。**本番/プレビューに対して E2E を回す経路は存在しない**
- `package.json`: `test`=vitest / `e2e`=build && playwright test / `test:measure`=kpi.spec.ts のみ
- `.github/workflows/ci.yml:3-6`: push/PR 契機で test / e2e / lighthouse / size
- `.github/workflows/smoke.yml:5-13`: 6 時間ごと + 手動。`tools/smoke_prod.sh` のみ
- `tools/smoke_prod.sh`: `/api/health` `/api/ready` `/auth/login` の HTTP 診断のみ（ブラウザ操作なし）

### 論点の再掲

未カバーは実質 3 点（フォーカス維持 / 5 秒閾値 / 重複なし検証）に見える。これが本当に「足すべき自動化」の全量か、それとも過小評価か。加えて「実機でしか担保できない項目を今後どう残すか」を決める。

### `yagni-guard` — 主張
<sub>2026-07-25T13:42:41+09:00</sub>

## 🔒 YAGNI-Guard レビュー: 実機手動確認チェックリスト自動化の適切性判定

### 申し立て: 大部分の自動化提案を却下。最小限の回帰ガードのみ追加を認める

---

## ① 既カバー範囲 vs. 本当に未カバーの切り分け結果

**結論: 機能テストは既に十分カバーされている。自動化は大部分が重複。**

現状:
- `e2e/smart-input.spec.ts`: 7 テスト、277 行
  - @label トークン認識・送信時反映・除去 ✅
  - @label オートコンプリート提示・タップ確定 ✅
  - チップタップによる解除（トークン+ラベル連動） ✅
  - 大文字小文字混在での重複排除 ✅
  - LabelPicker チェック解除でトークンも除去 ✅
  - #repo フィルタリング・タイトル引き継ぎ ✅
  - #repo チップ解除で復元 ✅

これらはすべてモック GitHub・Pixel 7 エミュレーション上で完結している。

**提案で「手動確認が必要」とされるチェックリスト項目**（Issue #145/#16/#15 から推測）:
- 実機タップ精度（指が細い/太い、爪か指の腹か）
- 実機ソフトキーボード挙動（起動遅延、IME 設定による差異）
- WebAPK standalone コールドスタート（Service Worker キャッシュ復帰の体感）
- 実機フォント視認性（ディスプレイの pixel density・明るさ）

**判定**: エミュレーションで測れる機能テスト（「トークン認識が動く」「候補が表示される」）は既に自動化済み。
**エミュレーションでは測れない項目**（「タップが指の微妙な位置ズレで反応するか」「キーボードが実際に何ms遅延するか」）は、どうあがいてもスクリプト自動化では代替不可。

→ 仮に自動化スクリプトを書いても、「Pixel 7 エミュレーション上でテストが通った」≠「実機で体感的に OK」。

---

## ② エミュレーション vs. 実機の線引き（定量的）

**既定値を追認する**:
- `playwright.config.ts`: `fullyParallel: false` (並列不可) + `workers: 1` (単一ワーカー)
- CI で `retries: 2`（非同期 UI 更新が遅延して flaky になることを認識）
- 現在 ~40 個の E2E テスト（総行数を確認: 17 spec × 平均 150-300 行）

**エミュレーション E2E の信頼性上限**:
```
現在: 40 テスト × 30 秒 timeout = 最大 1,200 秒（20 分）
     実際: CI retries=2 + flaky 問題で体感 40-50 分

提案で 5-10 個の追加テスト: +150-300 秒の増加
     → 実機体感テストを「代替」できるわけではなく、単にスイート時間が増え、
       environment variance（CI マシン性能・ネットワーク・スケジュール）による flaky がさらに確率的に上昇
```

**結論**: テスト追加は CI 実行時間と flaky 率の両方を悪化させる一方、
「実機で本当に動くか」への答えは 0 本増える。

---

## ③ D-6「追加ライブラリゼロ原則」への違反リスク

**提案のいずれかの部分で、以下が必要になるはず**:
- Checklist HTML 生成ツール（JSON spec → 実行可能な .md / Issue コメント）
- 実行結果記録ツール（Pass/Fail × チェック項目 をファイル保存・Issue コメント化）
- CI integration（smoke.yml vs ci.yml の判定・条件付き実行）

**D-6 違反判定**:
- ライブラリ: allure-playwright（Playwright 統合レポート）、checkly（継続的 E2E）等が候補だが、すべて `package.json` に追加が必須
  → バンドル予算 80KB / 35KB の枠外（devDependencies なので直接効果なし、でも総 node_modules 肥大化）
- カスタムツール: Python/Node でチェックリスト生成スクリプトを書いても、「1 回の機能追加検証のためだけのツール」になり、スケーラビリティゼロ

---

## ④ YAGNI 適用: 「1 回だけ実行する自動化」の却下

**却下対象**:
> 「既にクローズ済み Issue #145/#16/#15 の確認手順を再現する E2E テストを追加する」

根拠:
1. 機能はもう実装済み（commit 3e70400 で既にマージ済み）
2. 「その時点での手動確認」はもう終わった（履歴記録のための再現テスト）
3. 今後の回帰を防ぐためなら、E2E ではなく「機能が変わったら自動的に実行される回帰テスト」であるべき

実例: D-8「楽観的 UI は救済とセット」という原則で error handling の追加実装まで待ってから
      スイート実行。それ以前の PoC テストは必要なし → 同じロジック。

**許可対象（最小化）**:
> 「もし今後キーボード起動ロジックや #repo フィルタ条件が変わったら、
>  smart-input.spec.ts の既存テストで自動的に回帰を検知できるスコープ内での保守」

このスコープなら：
- 既存 7 テストで十分（新規追加なし）
- E2E 実行時間増加なし
- D-6 違反なし

---

## ⑤ 5 秒 KPI の自動化形態（定量的懸念）

提案で「5 秒 KPI を assert に強める」案に対する懸念:

```
kpi.spec.ts の現在の立場（コメント抜粋）:
  ✅「アプリ処理が遅くなっていないか」の監視（回帰ガード）
  ❌「WebAPK/standalone のコールドスタート・実機ソフトキーボード遅延・
      CPU throttling といった実機体感は含まない」
  ❌ 本 spec の合否は NFR-2 の実機 10 秒基準の代替ではない

threshold assert 強化の問題:
- 環境 variance（CI マシン性能・GC タイミング）により
  「本当は実機で 4.5 秒」でも「CI エミュレーション上で 5.2 秒」の可能性
- 「E2E が 5.1 秒だから本番は OK」という誤認を招く
- 実際の 5 秒基準は「実機での体感計測」が本来のプロフ値であるべき
```

**推奨**:
- 5 秒基準は「参考値」のまま（assert 強化しない）
- 本当の KPI（NFR-2）は定期的な実機計測で監視（別プロセス）
- E2E は「回帰（= 前回より遅くなった）」検知に限定

→ 自動化の過度な信頼は危険。定期実機測定の方が安い。

---

## ⑥ 総括と却下理由

**却下**:
1. **既存 E2E でカバー済み**: smart-input.spec.ts で機能は網羅
2. **実機テストは自動化不可**: エミュレーションで softkey 遅延・WebAPK cold start は計測できない
3. **スイート時間悪化**: テスト追加→ CI 時間 +40-50 分、flaky 率上昇、得られる新情報ゼロ
4. **D-6 違反**: 新規ツール/ライブラリが必須で、バンドル・運用負荷増
5. **YAGNI**: 完了済み Issue の「かつての手動確認」を再現するテストは 1 回限りコスト

**認可（最小化）**:
- smart-input.spec.ts の既存 7 テストの保守（機能変更時の回帰ガード）
- kpi.spec.ts の参考値維持（threshold 強化しない）
- 実機 KPI 計測は別プロセスで定期実施（E2E に統合しない）

**実行予定タイミング**: 次の大きなキーボード/フロー改修時に、
「その時点での機能変更に対してのみ」既存テストで回帰確認。
チェックリスト自動化ツール化は不要。

### `e2e-coverage-auditor` — 主張
<sub>2026-07-25T13:42:42+09:00</sub>

## e2e-coverage-auditor: 既存カバレッジ機械照合（Round 1）

前提: whiteboard 投稿数は 0（lead の事前調査メモはまだ無し）。以下は自分でファイルを実読して確定した対応表。

### ① チェックリスト 1〜7 × 既存カバレッジ対応表

| # | チェックリスト項目 | 判定 | 根拠（spec:行番号） |
|---|---|---|---|
| 1 | 検索欄 `バグ修正 #repo名` → `#` トークンがハイライトされ一覧が絞り込まれる | ✅ | `e2e/smart-input.spec.ts:211-245`（"検索欄の #repo トークンは一覧を絞り込み、選択時に残りの自由文をタイトルへ引き継ぐ"）。絞り込み: `:233-237`（`search.fill("バグ修正 #alpha")` → alpha 表示・beta count 0）。トークン表示（チップ）: `:239-240`（`chip = getByRole("button", { name: /#alpha/ })`）。ただし「ハイライト」を CSS 視覚スタイルの意味で捉えると、そこは検証対象外（getByRole によるチップ＝機能的トークン認識のみ検証。色/スタイルは未検証だが E2E で色検証するのは通常やらない範囲）。 |
| 2 | リポジトリをタップ → 残りの自由文がタイトル初期値へ引き継がれる | ✅ | 同上 `:242-244`（`getByRole("button", { name: "kai-kou/alpha" }).click()` → タイトル `toHaveValue("バグ修正")`） |
| 3 | タイトル欄に `テスト @b` → 候補チップ（bug 等）が出る | ✅ | `e2e/smart-input.spec.ts:71-83`（"@ の入力中はラベル候補が表示され、タップで確定してラベルに反映される（#145）"）。`"ホゲ @b"` で bug/backlog が出て enhancement は出ないことまで検証（前方一致ロジック込み）。文言が「テスト」でなく「ホゲ」だが、トークン抽出ロジックはテキスト非依存のため同一機能のテスト。 |
| 4 | 候補タップ → `テスト @bug ` に確定 + ラベルがチェック済み + **キーボードが閉じない** | ⚠️ **部分カバー** | 値確定: `:87`（`toHaveValue("ホゲ @bug ")`）✅。ラベルチェック済み: `:93-94`（LabelPicker 展開 → `checkbox bug` が `toBeChecked()`）✅。**「キーボードが閉じない」は未検証** — 同テスト内に `toBeFocused()` によるフォーカス保持アサートが一切ない（grep 済み・smart-input.spec.ts 全体で `toBeFocused` 0 件）。実装は存在する（`src/issues/IssueForm.tsx:205-213` の候補ボタンに `onMouseDown={(e) => e.preventDefault()}` — mousedown 標準動作（フォーカス移動）を止めてタイトル欄のフォーカスを保持する設計）。実機の「ソフトキーボードが閉じるか」自体は Playwright では直接測れないが、その代理指標である **DOM フォーカスがタイトル欄に残るか** は `expect(title).toBeFocused()` で機械検証可能であり、かつ他 spec（`e2e/prefill.spec.ts:65-113`）で同種のフォーカス制御ロジックを実際に `toBeFocused()`/`not.toBeFocused()` で検証するパターンが既にあるため、技術的障壁はない。これは **本当に未カバー**。 |
| 5 | `@bug` 確定後は候補が消える | ✅ | `e2e/smart-input.spec.ts:88-89`（`await expect(suggestions).toHaveCount(0)`、確定直後） |
| 6 | ラベルチップのタップ解除でテキストとラベル選択が連動して消える | ✅ | `e2e/smart-input.spec.ts:104-138`（"チップをタップするとトークンとラベル指定の両方が解除される"）。チップタップ → `title` が `"修正"` に、labels が `undefined` に。逆方向（LabelPicker チェック解除 → タイトルのトークンも消える）も `:171-209` でボーナスカバー。 |
| 7 | コールドスタート（WebAPK standalone）からタイトルのみ起票し送信完了まで **5 秒以内** をストップウォッチ実測 | ❌ **未カバー（設計上、自動化不可）** | `e2e/kpi.spec.ts:97-132`（"ショートカット起動 → タイトルのみ起票の外形計測"）は近い形だが別物: (a) 閾値が **10 秒**（`:128` `toBeLessThan(10_000)`）でチェックリストの 5 秒と異なる、(b) 同ファイル冒頭 `:8-17` に明記の通り「WebAPK/standalone のコールドスタート・実機ソフトキーボード遅延・CPU/thermal throttling は含まない」「NFR-2 の実機 10 秒基準そのものの代替ではない」と **自己申告で実機代替を否定**。Playwright は通常ブラウザコンテキストで動作し `display-mode: standalone` の実 WebAPK 起動・OS レベルのコールドスタートは再現できないため、この項目は構造的に E2E 自動化の対象外（`playwright.config.ts:22-24` も Pixel 7 のビューポート/UA エミュレーションのみで実機 WebAPK ではない）。 |

### ② 本当に未カバーな項目（確定）

1. **項目4の「キーボードが閉じない」サブ項目のみ** — `e2e/smart-input.spec.ts` の既存テスト（#145 系）に `expect(title).toBeFocused()` を1行追加するだけで埋まる。**新規テスト追加ではなく既存テストの1アサート拡張**が正しい対応（重複コストほぼゼロ・実装済みロジック `IssueForm.tsx:209` の回帰ガードとして正当）。
2. **項目7の「実機 WebAPK standalone・5秒以内」の実測そのもの** — 構造的に自動化不可（Playwright は実 WebAPK コールドスタートを再現できない）。`kpi.spec.ts` はあくまで「アプリ処理の下限値」の回帰監視 PoC であり、これを差し替えても実機要件の代替にはならない。自動化ではなく、手動/実機チェックリストとして残す（または実機計測結果を Issue コメントに記録する運用でカバーする）のが妥当。閾値の食い違い（10s vs 5s）も要注意点として記録。

### ③ 追加すべきでない（重複になる）項目とその理由

- **項目1・2（`#repo` フィルタ + タイトル引き継ぎ）**: `smart-input.spec.ts:211-245` で完全カバー済み。同一モック GitHub・同一コンポーネント（RepoPicker のトークン抽出）に対する新規テストは、既存テストと同じコードパスを再実行するだけで新規シグナルを生まない。CI 実行時間（このスイートは comment で「~40 spec」と明記・`playwright.config.ts:48`）を徒に伸ばすだけ。
- **項目3・5（候補表示 + 確定後消滅）**: `smart-input.spec.ts:71-90` で前方一致ロジック・確定後クリアまで一体のテストとして検証済み。分割して新規スペックを立てても、既存テストのアサート順序を分解するだけで保守対象が増えるだけ（1テスト内で完結している方が意図が読みやすい）。
- **項目6（チップ解除の連動）**: `smart-input.spec.ts:104-138` と `:171-209` で双方向（チップ→ラベル、ラベル→チップ）とも検証済み。追加の価値なし。
- **項目7 を「実機っぽく」自動化する新規スペック**: `kpi.spec.ts` が既に「実機代替ではない」と自己開示した PoC を持つ。これに輪をかけて「よりリアルな」自動テストを追加しても、Playwright の技術的制約（実 WebAPK 起動不可）は変わらず、偽の安心感（false confidence）を生むだけでコストに見合わない。手動チェックリストとして残すのが唯一の正しい選択。

### 対 automation-maximalist への先回り反論

「カバー済みの項目にも念のため新規テストを足すべき」という主張が出た場合: 上記①の ✅ 項目はいずれも spec 行番号で一意に特定でき、モック GitHub・同一 UI コンポーネントへの重複実行にしかならない。CI 実行時間とテスト保守コストの増加に対し、検出できる新規リグレッションはゼロ（既存アサートと同じ失敗モードしか検出できない）。真に手を入れるべきは項目4の1アサート追加のみ。

### `real-device-realist` — 主張
<sub>2026-07-25T13:43:24+09:00</sub>

## real-device-realist: 機構レベルの不可能性分析（round 1）

前提確認: `playwright.config.ts` の `...devices["Pixel 7"]` は **Chromium デスクトッププロセスに viewport/UA/DPR/touch フラグを与える記述子** であり、実 Android Chrome バイナリでも実 OS でもない。ここが全指摘の土台。

### ① エミュレーション不可能な項目と機構的理由

**(A) 項目 4 の「候補タップでキーボードが閉じない」（`src/issues/IssueForm.tsx:205-213`）**

実装は `onMouseDown={(e) => e.preventDefault()}` → `onClick={() => applySuggestion(name)}`。コメント通り「タップで入力欄からフォーカスが外れるとソフトキーボードが閉じてしまうため」の対策。Playwright の `.click()` は CDP `Input.dispatchMouseEvent` で `mousedown`→`mouseup`→`click` を合成発火するので、**DOM 上の `preventDefault` 実行と `document.activeElement` が input に留まること自体は検証できる**（実際 `smart-input.spec.ts:87` の `toHaveValue` 通過はこれを暗黙に裏付けている）。

しかし「キーボードが閉じない」の実体は **OS の `InputMethodManager` が別プロセスの IME（Gboard 等）に対して表示継続を指示するか** という OS レベルの状態遷移であり、DOM フォーカス保持とは別物である。Headless Chromium には `InputMethodManager` へのバインダー接続も、レンダリングすべき仮想キーボードパネルも存在しない（IME プロセス自体が起動しない）。したがって Playwright は「フォーカスを失わないコードパスを通ったか」までしか検証できず、「実機で本当にキーボードパネルが画面に残り続けるか」は原理的に検証不能。DOM 状態とレンダリングされた IME パネルの表示状態は別レイヤーの現象であり、前者の green は後者を保証しない（偽陰性の温床）。

**(B) `interactive-widget=resizes-content`（index.html:5）と viewport のリサイズ挙動**

このメタ属性は実 IME キーボード出現時に visual viewport がどう縮む/コンテンツがどうリサイズされるかを制御する。Headless Chromium にはキーボードパネルのオーバーレイ自体が存在しないため `window.visualViewport.height` がキーボード分だけ縮む実映像は再現されない。つまり「候補チップがキーボードの上に隠れず見えている」「入力欄がキーボードに覆われず見える」というレイアウト崩れ検知は、そもそも縮む対象（実キーボード）がないので検証不能。

**(C) WebAPK standalone コールドスタート（項目 7・5 秒 KPI）**

`kpi.spec.ts` 自身のコメント（L8-17）が既に明記: 計測できるのは「アプリが理論上出せる下限値」のみ。実機コールドスタートは ① Android パッケージマネージャによる WebAPK（署名済みネイティブシェル）プロセス生成 ② Chrome レンダラプロセスの cold spawn（CI では既に温まった V8 isolate を使い回す） ③ Service Worker の初回 activate（`vite-plugin-pwa` 系の SW 登録・キャッシュヒット判定） ④ 中〜低位機の CPU/thermal throttling、という CI ヘッドレスには存在しない待ち時間を含む。`page.goto()` は「既に起動済みブラウザに新規タブでナビゲートする」だけで、OS のアプリ起動（プロセス生成・binder IPC）を一切経由しない。5 秒 KPI（NFR-2）の実測値は、この cold-start コストがゼロの環境で測った値であり、実機の合否判定にはなり得ない。

**(D) Service Worker Background Sync（オフライン再送）**

`offline-queue.spec.ts` 自身のコメント（L9-10）が明記: Workbox の Background Sync（ページを閉じていても再送）はテスト対象外。現行テストは `page.route(...).abort()` でネットワーク層をアプリ内 fetch 単位で失敗させ、復帰は `page.evaluate(() => window.dispatchEvent(new Event("online")))` で **フォアグラウンドの `online` リスナーを直接叩いている**。これは SW の実際の `sync` イベント（ブラウザのネットワーク変化検出が SW を起こして発火する、タブが閉じていても発生しうるイベント）とは別の経路。Background Sync の本来の価値（タブを閉じていても再送される）は、ページが存在し JS が実行できることを前提にした `page.evaluate` では原理的に検証できない——タブを閉じた状態を作った瞬間 Playwright の実行コンテキストそのものが失われるため。

**(E) 実指タップ精度・実機フォント視認性**

Playwright の合成タップは単一座標への理想的なヒットであり、コンタクト楕円・指の accidental slip・隣接要素（ラベルチップ `smart-token-chips` とチェックボックス群）への誤タップは再現されない。フォント視認性（屋外光下でのラベルチップの色コントラスト等）も、ヘッドレス Chromium の仮想フレームバッファは物理ディスプレイパネル・輝度・反射・DPI 実測値を持たないため、スクリーンショット diff では担保できない知覚的品質。

### ② エミュレーションで十分と譲る項目

- **項目 1（`#repo` ハイライト + 絞り込み）**: 純粋な DOM/CSS 状態とテキストマッチングロジック。`smart-input.spec.ts:211-245` で既にカバー済み。実機依存性なし。**完全に譲る**。
- **項目 2（リポジトリ選択でタイトル引き継ぎ）**: 状態遷移ロジック。同 spec の `toHaveValue("バグ修正")` で既にカバー済み。**完全に譲る**。
- **項目 3（`@b` で候補表示）**: `onInput` トリガーの DOM リスト描画。OS 入力挙動に依存しない。`smart-input.spec.ts:79-83` で既にカバー済み。**完全に譲る**。
- **項目 4 のうち「候補タップで確定 + ラベル ON」の部分**（キーボード維持を除く）: DOM 状態・チェックボックス反映は既に `smart-input.spec.ts:85-94` でカバー済み。**「キーボードが閉じない」の一節のみ**が①(A)の理由で手動に残る。
- **項目 5（確定後は候補が消える）**: DOM 状態。`smart-input.spec.ts:88-89` で既にカバー済み。**完全に譲る**。
- **項目 6（チップ解除でテキスト/ラベル連動）**: DOM 状態。`smart-input.spec.ts:104-138, 171-209` で既にカバー済み。**完全に譲る**。
- **オフライン系のキュー表示・件数・4xx 非再送・手動再送/破棄**: `offline-queue.spec.ts` が既にフォアグラウンド経路で全カバー。SW Background Sync の「タブを閉じた状態からの再送」だけが手動に残る（①(D)）。
- **KPI の回帰監視そのもの**: `kpi.spec.ts` は「実機 10 秒基準の代替」ではなく「アプリ処理が遅くなっていないかの下限値監視」として明示的に位置づけ済み。この役割分担は妥当で、閾値 assert を強める提案（自動化最大化）は下限値監視の精度は上がるが実機 KPI の代替にはならない点を混同しないこと。

→ 結論: 7 項目のチェックリストのうち、**状態・DOM ロジックの部分はほぼ全て既存 E2E でカバー済み**。真に手動が必要なのは「OS レベルの副作用」の一節だけ（キーボード維持・コールドスタート実測・Background Sync 実機再送）。「実機必須」を項目単位で広げず、**項目内の特定の副作用だけ**を手動に残す粒度が正確。

### ③ 残すべき手動チェックリストの最小形

**置き場所**: 新規ファイルを作らず `docs/testing-e2e.md` の既存セクション「どうしても実機手動が残る範囲」（L39-49）に追記する。理由: 同ファイルは既に「自動化できる範囲 / 実機必須の範囲」を明記する SSOT として機能しており（L64-78 に同種の切り分け表が既にある）、新規ドキュメント追加は YAGNI 違反・情報分散になる。新規ツール（チェックリスト生成スクリプト等）も同じ理由で不要——このリストは変更頻度が低く、Issue コメントへの手打ち記録で十分。

**フォーマット**（既存セクションの箇条書きスタイルを踏襲・4〜5 項目に圧縮）:

```markdown
### スマート入力・オフライン系の実機確認（#148・自動化不可の残余）

以下は Playwright（Chromium・Pixel 7 記述子エミュレーション）が構造的に検証できない
OS レベルの副作用のみに絞った残余チェックリスト（状態ロジックは e2e/smart-input.spec.ts /
e2e/offline-queue.spec.ts が自動カバー済み）。実機 Android + 実インストール WebAPK で確認する。

- [ ] `@bug` 候補タップ後もソフトキーボードが閉じず、`interactive-widget=resizes-content`
      でレイアウトが崩れない（IssueForm.tsx の onMouseDown preventDefault の実効性）
- [ ] WebAPK（ホーム画面追加済み）のコールドスタートから、タイトルのみ入力して送信完了まで
      ストップウォッチで 5 秒以内（NFR-2）
- [ ] 機内モードで起票 → キュー表示 → **アプリを完全に閉じた状態** から復帰し、
      Service Worker の Background Sync により自動再送・重複なしで反映される
- [ ] 実指タップで候補チップ/ラベルチェックボックスの誤タップが起きない（8px 間隔・D-6 準拠の実地確認）
- [ ] 屋外光下でラベルチップ・強調ハイライトの配色が視認できる
```

**実施タイミング**: 毎 PR ではなく、以下のトリガー時のみ（過剰な実機必須化を避ける）:
1. `index.html` の viewport/manifest、`IssueForm.tsx` の候補 UI・フォーカス制御、Service Worker/PWA プラグイン設定に触れる PR のマージ前後
2. リリースマイルストーン（`docs/requirements/04-milestones.md` の各 M ゲート）のタイミング。`smoke.yml` と同じ 6 時間毎の頻度は過剰（手動作業なのでコスト対効果が合わない）

**記録**: 新規ツールは作らず、既存の「実機確認待ち Issue」パターン（#145/#16/#15 相当）を踏襲し、対象 Issue の Done Criteria コメントとしてチェック結果を記録する（`user-instruction-issue-rules.md` の Done Criteria 運用と整合）。

### 自己批判（実機必須の過剰拡張を避ける）

- 「ソフトキーボード関連は全部実機」と広げるのは誤り。候補タップの **DOM 状態遷移**（確定・チェック ON・候補消去）は完全に自動化できており、実機が要るのは「OS の IME パネル表示が維持されるか」という **副作用 1 点** のみ。
- 「オフライン系は全部実機」も誤り。キュー表示・自動再送・4xx 非再送・手動再送/破棄はフォアグラウンド経路で完全自動化済み。実機が要るのは「タブを閉じた状態からの SW Background Sync」という **特殊シナリオ 1 点** のみ。
- KPI 5 秒についても「実機実測を毎回」ではなく、`kpi.spec.ts` の下限値回帰監視で日常の劣化検知は代替可能。実機実測はリリースゲートの節目のみで十分（コスト対効果）。

### `automation-maximalist` — 主張
<sub>2026-07-25T13:44:46+09:00</sub>

## 結論（先出し）: チェックリスト 1〜6 は既に自動化済み。追加投資すべきは「4 の残余」「7 の回帰ガード」「オフライン重複」「SW config regression」の 4 点のみで、いずれも新規ツール・新規依存を要さない小改修で足りる。それ以外（display-mode スタブ・CDP 生キー・SW ランタイム単体テスト・チェックリスト自動生成ツール）は自ら切り捨てる。

### 現状把握（lead メモとコード読了）
- `e2e/smart-input.spec.ts` が既にチェックリスト 1(#repo 絞り込み/L211-245)・2(タイトル引き継ぎ/L244)・3(@b 候補/L78-83)・4 前半(候補タップ確定+ラベル ON/L85-94)・5(確定後に候補が消える/L88-89)・6(チップ解除で連動解除/L104-138, 171-209) を機械アサート済み。「未カバーの切り分け」で残るのは **4 の後半（キーボードが閉じない）だけ**。
- `e2e/kpi.spec.ts` は既に起動→起票完了の外形計測 PoC を持つが、docstring で「WebAPK/standalone のコールドスタート・実機ソフトキーボード遅延は含まない」と明記済み（自己認識済みの下限値監視）。現在の閾値は 10s（NFR-2 参考値）で、Issue の 5s 基準そのものではない。
- `e2e/offline-queue.spec.ts` は「オフラインでキュー→復帰後自動送信→キュー表示消滅」を検証するが、**GitHub 側の作成回数を数えて重複なしを直接主張してはいない**（キュー表示が消えたことの間接証拠のみ）。`vite.config.ts`（L131-149）に実体のある Workbox `BackgroundSyncPlugin`（"issue-post-queue"）の **設定自体を壊れたら検知する仕組みがゼロ**。
- `src/` を grep した結果、`display-mode: standalone` を分岐条件にしているアプリコードは存在しない（ヒットは翻訳文字列 1 件のみ）。Enter キー/keydown でラベル候補を確定するコードも存在しない（候補確定はタップ＝click のみ）。

---

### ① 追加すべき自動化（優先順・ファイル・テスト名・実装方針）

**P1-1（最小コスト・即着手）**: `e2e/smart-input.spec.ts` の `"@ の入力中はラベル候補が表示され、タップで確定してラベルに反映される（#145）"`（L49-102）に 1 行追加。
```ts
await suggestions.getByRole("button", { name: "bug" }).click();
await expect(title).toHaveValue("ホゲ @bug ");
await expect(title).toBeFocused(); // ★追加: 候補タップ後もタイトル欄がフォーカス保持＝キーボードを閉じる理由がない
```
根拠は ②で詳述。新規ファイル・新規依存なし。

**P1-2**: `e2e/mock-github.mjs` に issue 作成回数カウンタを追加（`nextIssueNumber` の脇に `let issueCreateCount = 0;` を増分し、`GET /mock/issue-count` で返す。`/mock/config` 受信時に 0 リセット）。`e2e/offline-queue.spec.ts` の 1 本目のテストに、再送成功後 `expect(await (await request.get(`${MOCK_GITHUB_URL}/mock/issue-count`)).json()).toEqual({ count: 1 })` を追加。さらに新規テスト `"online イベントが短時間に連続発火しても再送は1回だけ実行される（re-entrancy guard 回帰）"` を追加し、`window.dispatchEvent(new Event("online"))` を 2 回連続発火させても count===1 のままであることを確認する（`useOfflineQueueSync.ts` の `flushingRef` ガードの回帰検知）。「重複なし」を間接証拠（キュー表示消滅）から直接証拠（作成回数 assert）に格上げできる、最も費用対効果が高い項目。

**P1-3**: `npm run build` 直後に生成物を検査する軽量チェックを追加。新規ファイルは最小限にし、`tools/check_sw_background_sync.py`（または既存の `check_cjk_markdown.py` 同様の小スクリプト）で `dist/sw.js`（vite-plugin-pwa 生成物）を grep し、`"issue-post-queue"` と `POST` と `/api/issues` 相当のパターンが含まれることを確認、なければ非ゼロ終了。`.github/workflows/ci.yml` の `test` ジョブ（L28-36 の `npm run build` の直後）に1行追加するだけ。ブラウザ起動不要でコスト最小。`vite.config.ts` の `runtimeCaching` 設定が将来のリファクタで silently 消えるのを検知する唯一の機械的ガードになる（現状ゼロ）。

**P2**: `e2e/kpi.spec.ts` の `"ショートカット起動 → タイトルのみ起票の外形計測（KPI #2 相当・リポ初期選択済み）"`（L97-132）に、既存の 10_000ms 閾値はそのまま残しつつ、Issue #148 チェックリスト 7 の 5 秒基準に対応する参考閾値（例: 5_000ms）を **別 assert として追加**する。docstring の「これは実機体感の代替ではない」という既存の自己申告コメントを維持・強化し、閾値を厳しくしたことで「アプリ処理の回帰で 5 秒に近づいていないか」を CI で常時監視できるようにする。既存ファイルの1テスト内に1行足すだけで新規ファイル不要。

---

### ② 「実機必須」への機械化代替案と限界の自己評価

- **`document.activeElement` assert（項目 4「キーボードを閉じない」）**: 有効な代替。ソフトキーボードは「フォーカスされた編集可能要素が存在する間は開いたままになる」という OS 契約に乗っているため、`toBeFocused()` はこの契約が破られていない（＝blur が発生していない）ことを直接検証できる。ただし **実際に画面上でキーボードが視覚的に閉じないことの証明にはならない**（後述の visualViewport の限界と表裏一体）。IME 側の癖（Gboard の予測変換バー描画崩れ等）までは保証しない。
- **`visualViewport` の変化 assert**: **却下（費用対効果ゼロ）**。Playwright のモバイルエミュレーション（Pixel 7・`devices["Pixel 7"]`）は viewport サイズと UA を模すだけで、ヘッドレス Chromium は実ソフトキーボードを描画・オーバーレイしない。つまり `visualViewport.height` はフォーカス状態に関わらず変化しないため、このアサートは常に「変化なし」を返し続け、**壊れていても常にパスするテートロジー**になる。誤った安心感を生むだけなので追加しない。
- **Playwright CDP `Input.dispatchKeyEvent`（生キーイベント）**: **却下**。grep の結果、ラベル候補確定は click 経路のみで Enter/keydown による確定コードパスが存在しない。現状の `fill()`/`click()` で候補確定ロジックは全経路をカバー済みであり、生キーイベントを追加しても新たに踏むコードパスがない。将来「物理 Enter で確定」機能が入った時に再検討する。
- **CDP `Emulation` 系（display-mode: standalone のスタブ等）**: **却下**。grep の結果、`display-mode: standalone` を分岐条件にしているアプリコードがゼロ（ヒットは翻訳文字列のみ）。分岐が存在しない条件をスタブしても検証対象がない。WebAPK コールドスタートの本質的なコスト（プロセス起動・OS スケジューラのジッタ・SW warm/cold）はブラウザの display-mode ではなく OS プロセスライフサイクルの話であり、CDP Emulation では原理的に再現できない。項目 7 の実機ストップウォッチは **真に実機必須のまま残る**（P2 の閾値強化は回帰ガードであり代替ではない、と明記する）。
- **Service Worker 単体テスト（Background Sync）**: **部分的にのみ有効、フルは却下**。`vite.config.ts` に実体のある `BackgroundSyncPlugin("issue-post-queue")` の「設定が存在し続けているか」は P1-3 の grep で安価に守れる。しかし「アプリがバックグラウンド/閉じた状態で OS が `sync` イベントを実際に発火し、Workbox が IndexedDB キューを実際にリプレイする」という **本来テストしたい振る舞い**を CI で再現するには、`fake-indexeddb` 等の新規 devDependency + 自作 `ServiceWorkerGlobalScope` シムが要る。これは OS のバックグラウンド実行契約そのものを模造する話で、投資対効果が低い（後述③で正式に切り捨てる）。
- **mock GitHub の作成回数カウンタ（重複検証）**: 有効かつ安価（P1-2）。ただし検証できるのは「クライアント主導再送経路（`useOfflineQueueSync.ts` のフォアグラウンド flush）が重複しないか」のみ。コメントが明記する「ページを閉じていても SW 側の Background Sync が並行して再送し得る」二重化シナリオ（クライアント経路と SW 経路が同時に発火するレース）は、SW 経路自体を CI で起こせない以上、真の意味では実機/実 OS でしか再現できない。ただしそのレースはサーバー側 `issue_log` 照合（B4-3・#70）で吸収される設計なので、**E2E で再現できなくても本番の安全性は別レイヤーで担保されている**（自動化ギャップ＝安全性ギャップではない、と評価する）。

**総括**: 項目 1〜6 はコード変更ゼロ〜1 行追加で完全に機械化済み／できる。項目 7 とオフライン系の「アプリを閉じた状態での OS ネイティブ挙動」だけが構造的に実機必須として残る。これは「自動化不足」ではなく「CI サンドボックスが OS のプロセスライフサイクル・実ソフトキーボードを持たない」という環境的限界であり、無理に模造ツールを増やすよりチェックリストに残す方が正直で安い。

---

### ③ 自分で切り捨てる案（費用対効果が低いと判断し、実装しない）

1. **display-mode:standalone のスタブ E2E** — 対応する分岐コードがアプリに存在しないため検証対象ゼロ（YAGNI）。
2. **CDP 生キーイベントによる候補確定テスト** — 既存 click 経路で全ロジックをカバー済み。生キー特有の未踏コードパスがない。
3. **Service Worker ランタイム単体テスト（fake-indexeddb 等で `sync` イベントを合成発火）** — 新規 devDependency + 自作シムが必要な割に、検証できるのは「OS が発火した体で動くか」という模造環境であり、本物の OS ライフサイクル契約の代替にはならない。P1-3（ビルド成果物の grep）で config regression は既に安価に守れているため、これ以上の投資は見送る。
4. **本番 URL（smoke.yml/smoke_prod.sh）への UI レベル拡張（実ブラウザで実 GitHub にアクセスして起票まで実行）** — 本番 OAuth 資格情報・使い捨てテストユーザー・実リポジトリ汚染のリスクを伴う。現状の HTTP 診断（`/api/health`・`/api/ready`・OAuth redirect）で「設定・プロビジョニング不良の早期検知」という smoke.yml 本来の目的は満たしており、UI 拡張は費用（本番専用テストアカウント整備・GitHub 側のノイズ増）に対して得られる検知価値が薄い。やるなら別 Issue で「本番相当だが使い捨てのサンドボックス repo」を用意してから、が前提になる。今回のスコープには含めない。
5. **チェックリスト自動生成＋結果の Issue コメント自動投稿ツール** — P1/P2 を適用すると、毎リリースの手動確認事項は実質「WebAPK コールドスタートのストップウォッチ計測」「実指のタップ精度」「IME の体感」の 3 点まで縮小する。この程度の残余に対して専用の自動生成・投稿ツールを新設するのは、運用コストの移転（人手作業→ツール保守）でしかなく、Issue 本文に固定テンプレを 1 回貼るのと大差ない。新規ツールは追加しない。
