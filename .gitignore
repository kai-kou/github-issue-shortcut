node_modules/
dist/
.wrangler/
*.tsbuildinfo
.env
.env.local
.dev.vars
worker-configuration.d.ts
__pycache__/
*.pyc
coverage/
test-results/
playwright-report/
.DS_Store

# コスト telemetry（ローカル計測は継続・公開しない）
# 本リポジトリは public。運営者個人の Claude API 利用実額を公開しないため、以下は追跡しない。
# 過去分は 2026-07-28 に git 履歴から除去済み（#189・経緯は docs/rules/token-optimization-rules.md）。
# 同種の実額ログを再びコミットしないこと（集計が必要なら丸めた指標を別ファイルに置く）。
content/pipeline-state/cost_log.jsonl
content/pipeline-state/daily_cost_stats.json
content/pipeline-state/.cost_telemetry_pr_date
content/pipeline-state/.cost_telemetry_push_date
content/analytics/cost_monthly/

# Workers 利用状況テレメトリ（#235）。正本はデータブランチ telemetry/worker-usage 側で、
# ローカルのファイルは取り込み・集計用の作業コピーにすぎない。main で追跡すると、
# 無関係な feature 差分と未コミット検知にテレメトリの更新が毎回混ざる（上の cost_monthly と同じ理由）。
content/analytics/worker_usage/
